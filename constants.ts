import { Client, KPI, Task, PortfolioItem, CalendarEvent } from './types';

export const MOCK_CLIENTS: Client[] = [];

export const MOCK_KPI: KPI = {
  totalAUM: 0,
  activeClients: 0,
  pendingTasks: 0,
  monthlyCommission: 0,
};

export const MOCK_TASKS: Task[] = [];

export const MOCK_EVENTS: CalendarEvent[] = [];